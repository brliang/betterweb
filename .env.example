# Copy to backend/.env for local overrides. Every setting in backend/app/settings.py
# can be set here by its upper-case name.
DATABASE_URL=postgresql+psycopg://discovery:discovery@localhost:5432/discovery
# MAX_EXTERNAL_HOPS=2
